---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/Navigation.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/index.ts
  - src/app/layout.tsx
  - public/logo.png
autonomous: true

must_haves:
  truths:
    - "Navigation displays logo and links to Home, Services, About, Contact"
    - "Navigation is visible on every page"
    - "Footer displays contact info: address, phone, email"
    - "Footer displays placeholder social media links"
    - "Current page link is visually distinguished in navigation"
  artifacts:
    - path: "src/components/layout/Navigation.tsx"
      provides: "Header with logo and nav links"
      exports: ["Navigation"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with contact info and social links"
      exports: ["Footer"]
    - path: "src/components/layout/index.ts"
      provides: "Barrel export for layout components"
      exports: ["Navigation", "Footer"]
    - path: "public/logo.png"
      provides: "MadMedia logo image"
  key_links:
    - from: "src/components/layout/Navigation.tsx"
      to: "src/app/layout.tsx"
      via: "import and render in body"
      pattern: "<Navigation"
    - from: "src/components/layout/Footer.tsx"
      to: "src/app/layout.tsx"
      via: "import and render in body"
      pattern: "<Footer"
    - from: "src/components/layout/Navigation.tsx"
      to: "next/navigation"
      via: "usePathname for active state"
      pattern: "usePathname"
---

<objective>
Create Navigation and Footer layout components that provide consistent branding and contact accessibility across all pages.

Purpose: Navigation and footer establish the site's visual identity and ensure users can always find contact info. The logo and typography-forward nav must feel intentionally designed.

Output: Working Navigation (logo + 4 links with active state) and Footer (contact info + social placeholders) components integrated into root layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download logo and create layout components directory</name>
  <files>
    - public/logo.png
    - src/components/layout/index.ts
  </files>
  <action>
1. Download the MadMedia logo to `public/logo.png`:
   ```bash
   curl -L "https://images.squarespace-cdn.com/content/v1/67d8ced18226dc60625a0c27/0a31c5cb-90b2-4960-8b7e-d7d15c02181e/20240131_222722_B257B2C0-3867-4C38-AFDD-646637.png" -o public/logo.png
   ```

2. Create the layout components directory structure:
   ```bash
   mkdir -p src/components/layout
   ```

3. Create barrel export `src/components/layout/index.ts`:
   ```typescript
   export { Navigation } from './Navigation'
   export { Footer } from './Footer'
   ```
  </action>
  <verify>
- `public/logo.png` exists and is a valid image file
- `src/components/layout/` directory exists
  </verify>
  <done>
Logo downloaded to public folder. Components directory ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation component</name>
  <files>
    - src/components/layout/Navigation.tsx
  </files>
  <action>
Create `src/components/layout/Navigation.tsx`:

```typescript
'use client'

import Link from 'next/link'
import Image from 'next/image'
import { usePathname } from 'next/navigation'

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/services', label: 'Services' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' },
]

export function Navigation() {
  const pathname = usePathname()

  return (
    <header className="py-6 border-b border-primary/10">
      <nav className="mx-auto max-w-[var(--width-content)] px-6 flex items-center justify-between">
        <Link href="/" className="relative h-12 w-40">
          <Image
            src="/logo.png"
            alt="MadMedia"
            fill
            className="object-contain object-left"
            priority
          />
        </Link>

        <ul className="flex gap-8">
          {navLinks.map(({ href, label }) => (
            <li key={href}>
              <Link
                href={href}
                className={`
                  text-sm font-medium tracking-wide uppercase
                  transition-colors duration-300
                  ${pathname === href
                    ? 'text-accent'
                    : 'text-primary hover:text-accent'
                  }
                `}
              >
                {label}
              </Link>
            </li>
          ))}
        </ul>
      </nav>
    </header>
  )
}
```

Design notes:
- Uses 'use client' because usePathname requires client component
- Logo is 160x48px (w-40 h-12), positioned left
- Links are uppercase, tracking-wide for editorial feel
- Current page shows gold accent color
- Hover transitions to accent color
- Subtle bottom border separates from content
  </action>
  <verify>
TypeScript compiles without errors. Component exports correctly.
  </verify>
  <done>
Navigation component created with logo, 4 nav links, active state styling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Footer component and wire into layout</name>
  <files>
    - src/components/layout/Footer.tsx
    - src/app/layout.tsx
  </files>
  <action>
1. Create `src/components/layout/Footer.tsx`:

```typescript
import Link from 'next/link'

const socialLinks = [
  { href: '#', label: 'Facebook', placeholder: true },
  { href: '#', label: 'Instagram', placeholder: true },
  { href: '#', label: 'LinkedIn', placeholder: true },
]

export function Footer() {
  const currentYear = new Date().getFullYear()

  return (
    <footer className="bg-primary text-background py-section-sm mt-section">
      <div className="mx-auto max-w-[var(--width-content)] px-6">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-12">
          {/* Contact Info */}
          <div>
            <h3 className="font-display text-xl mb-4">Contact</h3>
            <address className="not-italic text-background/80 space-y-2 text-sm">
              <p>Madison, MN 56256</p>
              <p>
                <a
                  href="tel:320-204-5840"
                  className="hover:text-accent transition-colors duration-300"
                >
                  320-204-5840
                </a>
              </p>
              <p>
                <a
                  href="mailto:madmedia56256@gmail.com"
                  className="hover:text-accent transition-colors duration-300"
                >
                  madmedia56256@gmail.com
                </a>
              </p>
            </address>
          </div>

          {/* Navigation */}
          <div>
            <h3 className="font-display text-xl mb-4">Navigate</h3>
            <ul className="space-y-2 text-background/80 text-sm">
              <li>
                <Link href="/services" className="hover:text-accent transition-colors duration-300">
                  Services
                </Link>
              </li>
              <li>
                <Link href="/about" className="hover:text-accent transition-colors duration-300">
                  About
                </Link>
              </li>
              <li>
                <Link href="/contact" className="hover:text-accent transition-colors duration-300">
                  Contact
                </Link>
              </li>
            </ul>
          </div>

          {/* Social Links */}
          <div>
            <h3 className="font-display text-xl mb-4">Connect</h3>
            <ul className="space-y-2 text-background/80 text-sm">
              {socialLinks.map(({ href, label, placeholder }) => (
                <li key={label}>
                  <a
                    href={href}
                    className="hover:text-accent transition-colors duration-300"
                    aria-label={placeholder ? `${label} (coming soon)` : label}
                  >
                    {label}
                    {placeholder && <span className="text-background/40 ml-2">(coming soon)</span>}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <div className="mt-16 pt-8 border-t border-background/20 text-center text-sm text-background/60">
          <p>&copy; {currentYear} MadMedia LLC. All rights reserved.</p>
        </div>
      </div>
    </footer>
  )
}
```

2. Update `src/app/layout.tsx` to include Navigation and Footer:

```typescript
import type { Metadata } from 'next'
import { fraunces, dmSans } from '@/lib/fonts'
import { Navigation, Footer } from '@/components/layout'
import '@/styles/globals.css'

export const metadata: Metadata = {
  title: 'MadMedia - Social Media Management & Website Services',
  description: 'Professional social media management and website services for small businesses in Madison, MN and beyond.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${fraunces.variable} ${dmSans.variable}`}>
      <body className="flex flex-col min-h-screen">
        <Navigation />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
```

Design notes:
- Footer uses dark background (bg-primary) for visual separation
- 3-column grid on desktop, stacked on mobile
- Contact section has actual phone/email as links
- Social links marked as placeholders with "(coming soon)"
- py-section-sm uses 80px padding per design tokens
- mt-section adds 120px margin above footer
  </action>
  <verify>
1. Run `npm run dev`
2. Visit http://localhost:3000
3. Navigation should show logo on left, 4 links on right
4. "Home" link should be gold (active state)
5. Footer should show 3 columns: Contact, Navigate, Connect
6. Contact info should be clickable (tel: and mailto: links)
7. Social links should say "(coming soon)"
8. Copyright shows current year
  </verify>
  <done>
Navigation and Footer render on homepage. Logo displays. Links work. Contact info is correct. Social placeholders visible.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` runs without errors
2. Navigation shows logo and 4 navigation links
3. Current page link is highlighted in gold
4. Footer displays in 3-column layout on desktop
5. Contact info: "Madison, MN 56256", "320-204-5840", "madmedia56256@gmail.com"
6. Social links show "Facebook", "Instagram", "LinkedIn" with "(coming soon)"
7. Copyright shows current year
8. `npm run build` completes successfully
</verification>

<success_criteria>
- Navigation component displays logo and links consistently
- Footer component shows contact info and placeholder social links
- Both components are rendered on every page via root layout
- Active navigation state distinguishes current page
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
