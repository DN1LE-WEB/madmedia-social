---
phase: 04-polish-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/AnimateOnScroll.tsx
  - src/components/ui/index.ts
  - src/components/ui/Button.tsx
  - src/components/home/Hero.tsx
  - src/components/home/ServicesPreview.tsx
  - src/components/home/AboutPreview.tsx
  - src/components/services/SocialPackages.tsx
  - src/components/services/WebsiteServices.tsx
  - src/components/services/FAQ.tsx
  - src/components/about/BioSection.tsx
  - src/components/contact/ContactForm.tsx
autonomous: true

must_haves:
  truths:
    - "Headlines fade+scale in when scrolled 30% into view"
    - "Animations trigger once and do not replay on scroll back"
    - "Buttons lift slightly on hover with shadow"
    - "Staggered delays create intentional reveal sequence within sections"
  artifacts:
    - path: "src/components/ui/AnimateOnScroll.tsx"
      provides: "Reusable scroll animation wrapper component"
      exports: ["AnimateOnScroll"]
    - path: "src/components/ui/Button.tsx"
      provides: "Enhanced button with hover microinteraction"
      contains: "hover:translate-y"
  key_links:
    - from: "src/components/ui/AnimateOnScroll.tsx"
      to: "react-intersection-observer"
      via: "useInView hook"
      pattern: "useInView"
    - from: "src/components/home/Hero.tsx"
      to: "src/components/ui/AnimateOnScroll.tsx"
      via: "import and wrapper usage"
      pattern: "AnimateOnScroll"
---

<objective>
Add scroll-triggered entrance animations to headlines and CTAs, plus hover microinteractions on buttons.

Purpose: Create a polished, deliberate feel that looks unmistakably human-designed through crafted animations.
Output: AnimateOnScroll component, enhanced Button, all page components wrapped with entrance animations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-production/04-CONTEXT.md
@.planning/phases/04-polish-production/04-RESEARCH.md

@src/styles/globals.css
@src/components/ui/index.ts
@src/components/ui/Button.tsx
@src/components/home/Hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimateOnScroll component and enhance Button</name>
  <files>
    package.json
    src/components/ui/AnimateOnScroll.tsx
    src/components/ui/index.ts
    src/components/ui/Button.tsx
  </files>
  <action>
1. Install react-intersection-observer:
   ```bash
   npm install react-intersection-observer
   ```

2. Create `src/components/ui/AnimateOnScroll.tsx`:
   - 'use client' directive (uses hooks)
   - Import `useInView` from react-intersection-observer
   - Props: children, className (optional), delay (optional, default 0)
   - Use `useInView({ threshold: 0.3, triggerOnce: true })`
   - Apply inline styles for opacity (0 to 1) and transform (scale 0.95 to 1)
   - Use CSS transition with `var(--ease-out-expo)` from globals.css
   - Duration: 500ms (within 400-600ms range per CONTEXT.md)
   - Apply delay prop to transition-delay
   - Note: Reduced motion handling will be added in Plan 02

3. Export from `src/components/ui/index.ts`:
   ```typescript
   export { AnimateOnScroll } from './AnimateOnScroll'
   ```

4. Enhance `src/components/ui/Button.tsx`:
   - Update baseStyles to add hover lift effect
   - Add: `hover:translate-y-[-2px] hover:shadow-lg`
   - Change `transition-colors` to `transition-all` to animate transform + shadow
   - Keep existing color transitions
  </action>
  <verify>
    - `npm run build` succeeds
    - AnimateOnScroll.tsx exists and exports correctly
    - Button.tsx contains hover:translate-y
  </verify>
  <done>
    - react-intersection-observer installed
    - AnimateOnScroll component created with fade+scale animation
    - Button enhanced with hover lift and shadow effect
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply animations to all page components</name>
  <files>
    src/components/home/Hero.tsx
    src/components/home/ServicesPreview.tsx
    src/components/home/AboutPreview.tsx
    src/components/services/SocialPackages.tsx
    src/components/services/WebsiteServices.tsx
    src/components/services/FAQ.tsx
    src/components/about/BioSection.tsx
    src/components/contact/ContactForm.tsx
  </files>
  <action>
Apply AnimateOnScroll wrapper to headlines and CTAs in each component. Use staggered delays (0, 100, 200ms) within sections for intentional reveal sequence.

**Per CONTEXT.md: Animate headlines and CTAs ONLY, not every element.**

For each component, wrap ONLY these elements:
- h1, h2, h3 section titles
- CTA buttons and "Learn more" links
- Subheadlines directly after main titles

Do NOT animate:
- Body paragraphs
- Service cards/items
- Form fields
- Decorative elements

**Hero.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap h1 with AnimateOnScroll (delay=0)
- Wrap subheadline p with AnimateOnScroll (delay=100)
- Decorative line does NOT get animated

**ServicesPreview.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap h2 "What We Do" (delay=0)
- Wrap each "Learn more" link (delay=100, 200 for stagger)
- Service descriptions and icons do NOT get animated

**AboutPreview.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap h2 "Meet Julie" (delay=0)
- Wrap Button CTA (delay=100)
- Body paragraphs do NOT get animated

**SocialPackages.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap the main h2 title (delay=0)
- Do NOT animate individual package cards

**WebsiteServices.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap the main h2 title (delay=0)

**FAQ.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap the h2 title (delay=0)

**BioSection.tsx:**
- Add 'use client' directive
- Import AnimateOnScroll from '@/components/ui'
- Wrap h1/main title (delay=0)
- Wrap CTA button if present (delay=100)

**ContactForm.tsx:**
- Add 'use client' directive (likely already has it for form state)
- Import AnimateOnScroll from '@/components/ui'
- Wrap h1/main title (delay=0)
- Wrap submit button (delay=100)
- Do NOT animate form fields
  </action>
  <verify>
    - `npm run build` succeeds without errors
    - `npm run dev` and scroll through pages - headlines animate on scroll
    - Animations trigger at ~30% visibility
    - Animations only play once (scroll up/down to verify)
    - Body text and cards appear without animation
  </verify>
  <done>
    - All page components have AnimateOnScroll wrappers on headlines and CTAs
    - Staggered delays create intentional reveal sequence
    - Non-headline content remains static (no over-animation)
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - must complete without errors
2. Run `npm run dev` and test animations on each page:
   - Homepage: Hero headline, subheadline, Services title, Learn more links, About title, CTA button
   - Services: Package titles, FAQ title
   - About: Bio title, any CTAs
   - Contact: Form title, submit button
3. Test hover effects on all buttons - should lift with shadow
4. Scroll back up on a page - animations should NOT replay
</verification>

<success_criteria>
- Headlines fade in with subtle scale (0.95 to 1) when 30% visible
- Staggered reveals create deliberate, intentional feel
- Buttons lift 2px on hover with shadow effect
- Build passes without errors
- No console warnings related to animations
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-production/04-01-SUMMARY.md`
</output>
